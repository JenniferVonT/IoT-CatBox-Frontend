## Frontend environment variables

The frontend uses Vite environment variables in [frontend/.env](<./frontend/.env>) (local) and [frontend/.env.example](<./frontend/.env.example>) (template).

Only variables prefixed with `VITE_` are exposed to browser code.